[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tda"
version = "0.1.0"
description = "Trading Data Analysis"
dependencies = [
    "ccxt==4.2.89",
]

[project.optional-dependencies]
dev = ["pytest", "black", "ruff"]
pro = ["ccxt.pro"]